# Composition-Dependent Spectra

This experiment evaluates model-based chemically resolved MRI reconstruction for acetone-water mixtures with composition-dependent resonance frequencies using a linear approximation.

## Run

To run the experiment, use:
```bash
chmod +x run.sh
./run.sh
```

Results are written to `data/results/composition-dependent-spectra/`.
