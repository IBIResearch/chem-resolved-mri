# Ratio Series

Acetone-water molar ratio mapping across multiple ground-truth mixtures using model-based reconstruction (with an EPSI-like baseline in post-analysis).

## Run

To run the experiment, follow the following bash commands:
```bash
chmod +x run.sh
./run.sh
```

Results are written to `data/results/ratio-series/`.
