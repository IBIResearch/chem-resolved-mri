# Sequence Variations

This experiment corresponds to the appendix section on the impact of relaxation and evaluates variations in repetition time (TR) and flip angle.

## Run

To run the experiment, use:
```bash
chmod +x run.sh
./run.sh
```

Results are written to `data/results/sequence-variations/`. The analysis produces `tr_errors.pdf` and `fa_errors.pdf`.
